package:
  name: igor
  version: 0.1.0

source:
  path: ../..

build:
  number: 0
  # script: bash build.sh

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
    - make
  host:
    - gsl
    - llvm-openmp  # [osx]
    - llvm-tools
    - llvmdev 19.*
    - clangdev
    - doxygen ~=1.8
    - catch2 3.*
#   run:
#     - gsl
#     - llvm-openmp  # [osx]

test:
  commands:
    - igor -help

about:
  home: https://gitlab.com/igor-lab/igor
  license: GPL-3.0
  summary: Inference and Generation of Repertoires
